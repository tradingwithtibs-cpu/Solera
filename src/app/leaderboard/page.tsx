import { PeopleGrid } from "@/components/people/PeopleGrid";

/** People: the largest real holders of each tokenized stock, on the panel grid. */
export default function LeaderboardPage() {
  return <PeopleGrid />;
}
