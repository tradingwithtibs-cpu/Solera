import { ActivityStatic } from "@/components/portfolio/ActivityStatic";

/** /activity — every fill from the active ledger as one static panel. */
export default function ActivityPage() {
  return <ActivityStatic />;
}
