import { DiscoverGrid } from "@/components/discover/DiscoverGrid";

/** Discover: the feed (news and real fills) beside Trending. */
export default function DiscoverPage() {
  return <DiscoverGrid />;
}
